# Phase 1 checkpoint — 16 September 2026

Implemented on branch `drew/meeting-prep-intake-phase1` in commit `818f282` (Codex, lead implementer per Kevin's one-time role flip for this build): Worker/static intake UI, definitions schema, immutable GitHub submission gateway, recurring carry-forward, and the locked-record renderer handoff. No Worker was deployed and no Cloudflare setting was changed.

**Drew's review pass (same day), commit `d05c39e`:** independently re-ran the Worker test suite (3/3 pass) and the renderer against `test/fixtures/submitted-intake.json` (screenshotted real output — correct fixed-grid 3-column brief, real Oxford crest, tone framing applied) rather than trusting Codex's self-report on faith. Found the implementation genuinely complete but written as dense single-line functions; ran a formatting-only `prettier` pass on the 5 JS/CSS/test files (no logic change, tests re-verified after). Also screenshotted the static UI served locally.

**Status:** PR #10 approved by Kevin and merged to `main` at commit `067bc5e1b391d9c52c26a75aa7121ff1becaa490` (16 Sep 2026). Not deployed — no `wrangler deploy` has been run, no Cloudflare Worker exists for this yet.

Validation: run `node --test meeting-prep-intake/test/worker.test.mjs`; renderer smoke test requires the existing `brief_chrome.py` scratch font/crest assets and can use `test/fixtures/submitted-intake.json`. Re-ran the unit suite post-merge (3/3 pass) and additionally ran a live in-process smoke test — imported the real exported `fetch` handler directly in Node (no `wrangler dev` needed) and exercised every route against the **live** GitHub API using a synthetic, clearly-labelled disposable ad-hoc intake, immediately deleted after. All 7 checks passed: list meetings, previous-intake lookup on a nonexistent meeting (clean null), a fresh submit (201), a duplicate submit without `supersedes` (409 lock), a revision with a wrong `expectedSha` (409 conflict, nothing overwritten), a revision with the correct `expectedSha` (201, `supersedes` recorded), and a disallowed CORS `Origin` (403). No trace left in the repo (see `begb0037admin/drew` memory, `meeting-prep-intake-merge-and-deploy-prep-16sept.md`, for the full log).

**Deploy prep — two genuine one-time human actions outstanding, not yet done:**
1. Worker secret `GITHUB_PAT` not set. Needs a fresh fine-grained GitHub PAT (Contents read/write, this repo only) — no API can create a fine-grained PAT, this is a GitHub-UI-only action. Kevin should create it and run `wrangler secret put GITHUB_PAT` himself in `meeting-prep-intake/` so the secret never has to be pasted anywhere else.
2. Cloudflare Access Application not created. Live-tested: the account's current wrangler OAuth token gets `auth.forbidden` trying to create one (it can only list/read). Needs a Cloudflare API token scoped to "Access: Apps and Policies: Edit", created once via the Cloudflare dashboard — then Drew can provision the Access Application (and future ones) without further manual steps.
3. No deploy hostname decided yet — `wrangler.toml` has no route/custom domain configured. Proposed `meeting-prep.lelitte.co.uk` (matches the estate's existing `finance.`/`hub.`/`org.`/`ai-usage.` `lelitte.co.uk` convention); needs Kevin's confirmation.

Exact next action: relay the above three items to Kevin. Once the hostname is confirmed and both credentials exist, Drew sets `ALLOWED_ORIGIN`, runs `wrangler deploy`, creates the Access Application, and re-verifies with a live post-deploy check through the real Access-gated hostname.
