# HANDOVER — 2026-09-07 (August 2026 KPI run complete; open: AI-fingerprint scrub + gate, caption round-trip visual)

## TL;DR
August 2026 KPI Presentation is **built, verified, delivered, and checkpointed.** Final canonical
`...\2026\08 Aug\KPI presentation - August 2026.pptx` — md5 **`215162c9ccbd8a19bc76ddf5ef0b8e95`**, 2,275,660 B — rebuilt on `7fe23f2` (auto-loads the speaker-notes markdown; scope captions hosted as durable inherited placeholders that survive a PowerPoint open/save; stale/empty-notes gate in `validate_deck()`). Slides 2–10 carry the August speaker notes; figures byte-identical to every prior verified build (0 table/chart/image diff across `dcb3e67e` → `38d68fa2` → `215162c9`); `validate_deck()` PASS.

**Closed this session:** the Slide 5 Incident-Other methodology (ADR-0001 — Slide 5 `Table 4` = 9 named FA rows + "Other", Total/% base = full source month total, so Slide 5 ties to Slide 7; Slide 4 differs by design + on-slide captions); the speaker-notes process gap (notes were June's, carried verbatim through July and August — now authored fresh, cross-checked, pipeline auto-loads + gates); scope-caption durability (re-homed from `add_textbox` to inherited placeholder).

**Standing rule added this session (Kevin) — see `KPI_RUN_SOP.md`:** *"do not leave any sign of AI on any decks — this is not an option, any notes/mentions must be removed, this is standing process."*

## STANDING RULE — no sign of AI on the deck
Every KPI deck must carry zero AI-identifying content anywhere in the `.pptx` package — speaker notes, slide/shape text, title/subtitle/footer, `docProps` (core/app/custom), **image alt-text (`descr=`) on every slide AND every layout/master**, comments, embedded-object metadata. Disqualifying strings (non-exhaustive): `claude`, `anthropic`, `chatgpt`, `openai`, `copilot`, `codex`, `AI-generated`, `generated by AI`, `language model`, `LLM`. To be enforced by `validate_deck()` (see "Next Concrete Action" b).

## AI-signature audit of the delivered canonical (md5 `215162c9…`) — 7 Sep 2026
- **Speaker notes (Slides 2–10):** CLEAN — no AI mention.
- **Visible slide text + titles:** CLEAN.
- **`docProps`:** `dc:creator` = "Dave Startup" (the Oxford OU template's author), `cp:lastModifiedBy` = "Kevin Lelitte". No AI tool. CLEAN.
- **FOUND — not yet scrubbed:** ~19 files under `ppt/slideLayouts/` carry a Microsoft auto image alt-text: `descr="A close-up of a sign  AI-generated content may be incorrect."` — inherited from the Oxford OU base template, **not** added by our build, not visible on any slide, but present in the package. Fails the standing rule. Must be removed (see "Next Concrete Action" a).

## Next Concrete Action (for the next session)

**a. Scrub the AI alt-text and rebuild.** In the base deck's package (`ppt/slideLayouts/*.xml`, and check `ppt/slideMasters/`, `ppt/slides/` too), remove the `descr="…AI-generated content may be incorrect…"` alt-text (and any other AI-identifying `descr=` / text). Cleanest: fix it in whatever base `.pptx` the monthly build copies forward (so it doesn't keep re-propagating), then rebuild the canonical August deck via `build_month(2026, 8)` on the then-current pipeline. Re-verify: **zero figure diff** vs `215162c9…` (every table cell / chart series / chart image), `validate_deck()` PASS, and the Codex speaker-notes review still CLEAN.

**b. Add a permanent `validate_deck()` check (Drew).** Scan, and **hard-fail the build on any hit** (outside binary fonts/media): speaker notes + all slide/shape text + `docProps` core/app/custom + every slide's and every layout's/master's image alt-text (`descr=`) + comments, for AI-identifying strings (`claude`, `anthropic`, `chatgpt`, `openai`, `copilot`, `codex`, `AI-generated`, `generated by AI`, `language model`, `LLM`, case-insensitive; keep the list extensible). Add a tampering test (inject one string into a `descr=` and a notes slide — build must fail).

**c. Caption round-trip visual confirm (Kevin).** One real PowerPoint open → close → reopen of the canonical deck to confirm the Slide 4 pointer and Slide 5 scope caption (now inherited-placeholder shapes) both survive. python-pptx save→reload×3 already keeps them; a live PowerPoint confirmation is the last check.

**Backlog:** populate `docs/reference/kpi-definitions.md` (still a stub) using ADR-0001 + the reconciliation reference.

## Watch Out For
- The delivered on-disk canonical was written by our build; if Kevin opens it in PowerPoint before (a) is done, PowerPoint may re-save it and (older builds) drop captions — the `7fe23f2` placeholder captions are round-trip-safe, but confirm via (c).
- Figures are locked: `215162c9…` is byte-identical on every table/chart/image to `dcb3e67e` (the first verified build). Any rebuild must reproduce that exactly — a figure diff is a stop-and-report.
- Do not "fix" the Slide 7 "LESS THAN 5 DAYS" 0.01 pp cell — it's a documented source rounding-methodology ambiguity, ≤0.01 pp permitted on that cell only.
- Minor pre-existing cosmetic (not a regression): Slides 8 & 9 matplotlib "Total:" annotation slightly overlaps the last bar label — same every month.
- "Interfaces" is folded into "Other" on Slide 5 (ADR-0001 §2) — an 11th row to keep both named is a layout change, deferred.
- The monthly run has a **mandatory ordered checklist** (build → notes → Codex per-note verification → Kevin) in `KPI_RUN_SOP.md`. A numerically-valid deck with stale notes, or with any AI fingerprint, is **not** a complete run.
