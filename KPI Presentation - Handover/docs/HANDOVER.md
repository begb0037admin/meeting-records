# HANDOVER — 2026-09-07 (August 2026 delivered incl. speaker notes; two known issues handed to Drew)

## TL;DR
August 2026 KPI Presentation delivered as canonical (`fcde319`) after Drew's Slide 5 fix + scope captions + hardened `validate_deck()` (`b9826fd` → `503e274`). **A process gap then surfaced: the deck's Slides 2–10 speaker notes were still June's, carried verbatim through July and August** — `populate_deck()` never touched `notes_slide`; the gate only checks tables/charts, so it was invisible. Kevin reads these aloud presenting. **Now fixed for August:** Lauren authored `notes/speaker-notes-2026-08.md` (voice/structure matched to March–June decks, every figure cross-checked), and wrote the 9 notes into the canonical deck — before/after data diff = 0, `validate_deck()` PASS. **Final canonical: md5 `38d68fa2502b5fcdbb54f5ae411a315f` / 2,275,746 B.** Process is now an explicit mandatory SOP step (`KPI_RUN_SOP.md` §2).

**Two issues handed to Drew — both now DONE in the pipeline (7 Sep 2026, commit the 7 Sep 2026 notes-handling + caption-durability commit):**
1. **Notes handling + stale-notes gate — DONE.** `build_month()` now loads `KPI Presentation - Handover/notes/speaker-notes-YYYY-MM.md` via `load_month_notes()`, writes each `## Slide N` into `notes_slide`, and **hard-fails (`FileNotFoundError`) if the file is missing**. `validate_deck()` now checks Slides 2–10 notes: non-empty, **not byte-identical** to the base deck's notes, current month named across them (all hard fails), plus a non-blocking per-slide "prev-figure-present / current-figure-absent" advisory. Self-test `ALL PASS` (June fixture + fresh August on Lauren's committed notes); 4 tampering tests block the build.
2. **Scope captions now survive a PowerPoint open/close — DONE.** They are no longer free `add_textbox` shapes (which PowerPoint dropped). They are now hosted as an **inherited BODY placeholder** (the "Title w Chart" layout's spare idx-11 placeholder) — a placeholder is the shape class PowerPoint is built to retain. The gate's caption check changed from "a shape named X exists" (absolute) to "the explanatory text is present somewhere on the slide" (content signature) — a rename / re-home is tolerated, the text being **gone entirely is a hard fail**. python-pptx save→reload×3 keeps both as `is_placeholder=True idx=11`; a real PowerPoint open/close still wants one confirmation from Lauren/Kevin.

## THE MONTHLY KPI RUN — MANDATORY ORDERED CHECKLIST

Hard-coded (Kevin, 7 Sep 2026: *"let's hard code this… this is now the fourth month… the process is still flaking"*). Do these in order, every month; each step gates the next. Full detail in `KPI_RUN_SOP.md`.

1. **BUILD THE DECK** — `build_month(YYYY, M)`. `validate_deck()` runs inline **before the file is written**; any failure ⇒ raises, **no** deck (`.REJECTED` only), non-zero exit. Also hard-stops if the month's speaker-notes file is missing.
2. **DRAFT / UPDATE SPEAKER NOTES (Lauren)** — `notes/speaker-notes-YYYY-MM.md`, `## Slide N` for Slides 2–10. Build won't run without it. Automated notes check must pass (non-empty, not byte-identical to base, current month referenced).
3. **CODEX INDEPENDENTLY VERIFIES EVERY SPEAKER-NOTE NUMBER — mandatory, every month, a required step (not optional QA).** Codex re-derives every figure / % / month / direction word / KPI-band colour in the notes from that slide's own table **and** the source data, PASS/FAIL per claim. Invoke `codex exec -s read-only --skip-git-repo-check` from the repo root with a prompt of the shape in `docs/reference/codex-notes-review-prompt.md`. Record run + verdict in `docs/sessions/YYYY-MM-KPI-run.md`. `NOTES NOT CLEAR` ⇒ back to Lauren/Drew, do not work around it.
4. **ONLY THEN → KEVIN** — deck to Kevin / on to Michael O'Sullivan only after 1–3 pass and are logged.

## State of Play
- **Delivered canonical:** `...\2026\08 Aug\KPI presentation - August 2026.pptx`, built on `503e274`, `validate_deck()` PASS inline, all figures verified (see `docs/sessions/2026-08-KPI-run.md`).
- **File now open/locked:** confirmed live — `POWERPNT.EXE` running, the file rejects even a read-only exclusive open, and its on-disk md5/size **changed** since delivery (delivered `dcb3e67e…`/2,276,077 B → now `433369ca…`/2,132,545 B, last write 18:43:44) — consistent with Kevin opening it in PowerPoint and it auto-saving/re-saving once. **This means the live canonical file is no longer byte-identical to what Lauren built** — any future automated rebuild must not silently overwrite whatever Kevin has open/changed. Nothing has been forced; Lauren is waiting for the lock to clear before writing notes.
- **Speaker-notes gap, root cause:** `populate_deck()` copies the base deck (previous month's finished file) and only ever edits tables/charts/captions — it has no code path that touches `notes_slide` at all, so notes just ride along from whatever the base deck last had. Confirmed live: July's and August's `notes_text_frame` content is byte-identical to June's.
- **Established notes pattern (read March–June 2026, not just July):** per-slide narrative — the month's headline figure, MoM movement (with context on whether the comparison month was itself high/low), YoY where available, KPI threshold/colour band where relevant, and a set of standing lines repeated verbatim or near-verbatim month to month: H&S "still building the dataset for H&S, so the trend will become clearer as the year goes on"; H&S 6+ day cases "waiting on the customer for information, or on a third-party to respond… not sitting idle with us"; Slide 5's WFM-complete/all-departments-live/settled-BAU-footprint framing for Service Requests, and the matching "ongoing demand we should expect to carry each month" line for Incident-Other; Slides 6/7/10's small-team/no-headroom/no-slack framing; Slide 8 "consistently green throughout 2026… positive reflection of the team"; Slide 9's fixed historical peak-trend anchors — **8.3 days Feb 2025, 6.5 Oct 2025, 4.1 Jan 2026** — reused every month regardless of whether they're inside that month's own rolling 15-month chart window. March/April also carried a "This slide shows…" descriptive opener and an "ANTICIPATED QUESTIONS" Slide 11 block; both were dropped by May/June and are not reinstated here (matching the most recent established practice, not the oldest).
- **August notes drafted, cross-checked, committed:** `KPI Presentation - Handover/notes/speaker-notes-2026-08.md` — 9 sections (Slides 2–10), each figure checked against the built August deck's own tables and the underlying source workbook (full cross-check table in `docs/sessions/2026-08-KPI-run.md`). Slide 5's note reflects the corrected Incident-Other picture (total 61, the "Other" row, the Slide 4-vs-5 scope note) — it does not reproduce the stale "68 tasks" framing from the pre-ADR-0001 methodology. Parses cleanly against Drew's `load_month_notes()` loader (tested against his in-progress, uncommitted script).
- **Pipeline wiring (Drew — LANDED the 7 Sep 2026 notes-handling + caption-durability commit):** `NOTES_DIR`, `NOTES_REQUIRED_SLIDES` / `EXPECTED_NOTES_SLIDES` (0-based 1–9 = Slides 2–10), `notes_path_for()`, `load_month_notes()`, `notes=` param on `populate_deck()` (writes each `## Slide N` into `notes_slide`, captures `prs._kpi_base_notes` first), `build_month()` hard-stops with `FileNotFoundError` if the notes file is missing, and `validate_deck()` has a full speaker-notes section (empty / byte-identical-to-base / month-not-referenced = hard fail; per-slide stale-figure = non-blocking advisory; `notes_required=False` for reload audits). June fixture `notes/speaker-notes-2026-06.md` added (real historical June notes, for self-test coverage). Captions re-homed to inherited placeholders in the same commit. Self-test `ALL PASS`; tampering tests confirm each new check blocks the build.

## Next Concrete Action
The August KPI run is complete (deck + notes). Pipeline hardening for both handed-to-Drew issues has landed (the 7 Sep 2026 notes-handling + caption-durability commit). Remaining:
1. **Codex** independently verifies every August note's figures against its slide + source — checklist step 3, still to run for August.
2. **Lauren, once the canonical file unlocks:** rebuild `build_month(2026, 8)` on the 7 Sep 2026 notes-handling + caption-durability commit (or targeted-write) so the delivered file gets the **durable placeholder captions** + notes in one pass; diff-verify tables/charts unchanged; re-run `validate_deck()`. Until then the on-disk canonical may still be missing the Slide 4 caption from Kevin's earlier open.
3. **Kevin:** send the deck to Michael O'Sullivan; decide the July 2026 deck question (see Watch Out For).
4. **Backlog:** populate `docs/reference/kpi-definitions.md` (still a stub) using ADR-0001 + the reconciliation reference.

## Watch Out For
- **Scope-caption durability: fixed in the pipeline (the 7 Sep 2026 notes-handling + caption-durability commit) but not yet re-applied to the on-disk canonical.** New builds host the captions as inherited placeholders (PowerPoint keeps those). The delivered file was built before this fix, so if Kevin opened it and PowerPoint dropped the Slide 4 caption, that file still needs the rebuild/re-write in "Next Concrete Action" #2. The gate now hard-fails if the caption *text* is absent from a slide (rename/re-home tolerated).
- Final canonical md5: `38d68fa2502b5fcdbb54f5ae411a315f`, 2,275,746 B. Figures byte-identical to the delivered `dcb3e67e` build (verified); the only adds are the 9 speaker notes.
- July 2026 deck already sent to Michael O'Sullivan still has the old Slide 5 numbers (Total 62) **and** stale (June's) speaker notes. **Kevin's call, still open:** reissue the deck, or a written explanation citing ADR-0001. He raised the Slide 5 discrepancy 13 Aug — a reply is owed. The July deck's notes issue wasn't separately raised by Michael but exists.
- "Interfaces" is folded into "Other" on Slide 5 (ADR-0001 §2) — an 11th row to keep both is a layout change, Drew's, deferred.
- Do not "fix" the Slide 7 "LESS THAN 5 DAYS" 0.01 pp cell — registered D4.
- Minor pre-existing cosmetic (not a regression): Slides 8 & 9 matplotlib "Total:" annotation slightly overlaps the last bar label — same every month.
- Going forward, per `KPI_RUN_SOP.md` §2: a numerically-valid deck with stale notes is **not** a complete KPI run. Do not let this slip because `validate_deck()` is green — it does not check notes.
