# Support Staff Contract URL Updates

Put the two files for each update in `Input`:

- the current contract template to be updated;
- the associated tracked-changes or reference document that defines the URL replacements.

Then say: **Okay, let's update the template file.**

The original template remains untouched. The dated updated copy and its verification report will appear in `Output`.
